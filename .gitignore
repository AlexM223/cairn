node_modules

# Build output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Logs
*.log
npm-debug.log*

# Editors / local tooling
.vscode/*
!.vscode/extensions.json
.idea/
.claude/

# Runtime data (SQLite database)
data/

# Load-test harness output (cairn-h2of / cairn-3h0v) — server logs, per-run
# JSON results, sessions.json (bearer tokens for a throwaway DB, but no sense
# keeping them around either).
scripts/load-test/results/
